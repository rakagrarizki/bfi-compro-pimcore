stages:
  - build
  - deploy
build_staging:
  before_script:
    - chmod +x .ci/build.sh
  stage: build
  script:
    - .ci/build.sh
  only:
    - development
  allow_failure: true
deploy_staging:
  stage: deploy
  before_script:
    - chmod +x .ci/run.sh
  script:
    - .ci/run.sh
  environment:
    name: staging
    url: http://bfi.staging7.salt.id
  only:
    - development